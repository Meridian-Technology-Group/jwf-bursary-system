name: CI

on:
  pull_request:
  push:
    branches: [main, staging]

jobs:
  ci:
    name: Lint, typecheck, test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22.12.0'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # Prisma validate/format require DATABASE_URL + DIRECT_URL to be
      # *defined* (not reachable). Dummy values are enough.
      - name: Prisma schema validation
        env:
          DATABASE_URL: postgresql://x:x@localhost:5432/x
          DIRECT_URL: postgresql://x:x@localhost:5432/x
        run: npx prisma validate

      - name: Prisma schema formatting
        env:
          DATABASE_URL: postgresql://x:x@localhost:5432/x
          DIRECT_URL: postgresql://x:x@localhost:5432/x
        run: npx prisma format --check

      # Added 11 Sep 2026 after a generated INSERT lost its terminating
      # semicolon: CI was green, the failure appeared only at migrate deploy
      # AFTER merge, and left the migration blocking every later deploy.
      - name: Migration SQL statement terminators
        run: node scripts/check-migration-sql.mjs

      - name: Lint
        continue-on-error: true
        run: npm run lint

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Test
        run: npm test
