# Open-Source Manifest

> **STATUS: STUB** — MSA clause 12.2 deliverable. To be completed before
> go-live (B15).

## Purpose
A manifest of third-party open-source dependencies and their licences, so the
Foundation has a clear record of what ships in the system.

## To complete
- [ ] Generate from production dependencies (e.g. `license-checker --production`).
- [ ] Table: package, version, licence (SPDX), and link.
- [ ] Flag any copyleft / non-permissive licences for review.
- [ ] Note the regeneration command so the manifest can be refreshed per release.
